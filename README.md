# janus-cli
cli tool to perform recursive literal find/replace on strings in filenames, directory names, and text file contents.

This script provides a command-line interface to perform recursive find and replace operations on strings in filenames, directory names, and text file contents. 
It includes options for previewing changes and renaming the root directory.
